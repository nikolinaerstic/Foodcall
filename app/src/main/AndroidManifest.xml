<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.nikolina.restaurantapp">



    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".cart.CartActivity"
            android:label="Košarica"></activity>

        <activity android:name=".menu.MenuActivity"
            android:label="Jelovnik"></activity>

        <activity android:name=".menu.DesertMealActivity"
            android:label="Desert"></activity>

        <activity android:name=".menu.DrinkActivity"
            android:label="Pića"></activity>

        <activity android:name=".menu.StartMealActivity"
            android:label="Predjelo"></activity>

        <activity android:name=".menu.MainMealActivity"
            android:label="Glavno jelo"></activity>

        <activity android:name=".detail.DrinkDetailsActivity"
            android:label="Detalji"></activity>

        <activity android:name=".detail.StartDetailsActivity"
            android:label="Detalji"></activity>

        <activity android:name=".detail.DesertDetailsActivity"
            android:label="Detalji"></activity>

        <activity android:name=".detail.MainDetailsActivity"
            android:label="Detalji"></activity>

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyBZBhXhYjL4p_VaOnLOuVWjSh0n-k2ObZ8"/>

    </application>

</manifest>